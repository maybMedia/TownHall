<?xml version="1.0" encoding="utf-8" ?>
<local:PageWithNavBar xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:TownHall"
             x:Class="TownHall.Buy"
             Title="Buy">
	<VerticalStackLayout Padding="10" Spacing="10">
		<SearchBar x:Name="Search"
                   Placeholder="Search for a listing"
                   SearchButtonPressed="OnSearchBarButtonPressed"/>

		<CollectionView x:Name="ItemCollectionView" Margin="10" ItemsSource="{Binding DisplayedItems}">
			<CollectionView.ItemsLayout>
				<GridItemsLayout Orientation="Vertical"
                             Span="4"
                             HorizontalItemSpacing="10"
                             VerticalItemSpacing="10"/>
			</CollectionView.ItemsLayout>
			
			<CollectionView.ItemTemplate>
				<DataTemplate>
					<Frame CornerRadius="10" Padding="10" Margin="5" BackgroundColor="White" HasShadow="True">
						<Frame.GestureRecognizers>
							<TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type local:Listings}}, Path=CardTappedCommand}"
                                          CommandParameter="{Binding .}" />
						</Frame.GestureRecognizers>
						<StackLayout>
							<Image Source="{Binding ImageUrl}" HeightRequest="150" Aspect="AspectFill"/>
							<Label Text="{Binding Name}" FontAttributes="Bold" FontSize="Medium" Margin="0,5,0,0"/>
							<Label Text="{Binding Price, StringFormat='{0:C}'}" FontSize="Small" TextColor="Gray"/>
						</StackLayout>
					</Frame>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>
	</VerticalStackLayout>
</local:PageWithNavBar>