<?xml version="1.0" encoding="utf-8" ?>
<local:PageWithNavBar xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:TownHall"
             x:Class="TownHall.Buy">
	<VerticalStackLayout Padding="10" Spacing="10">
		<SearchBar x:Name="Search"
                   Placeholder="Search for a listing"
                   SearchButtonPressed="OnSearchBarButtonPressed"/>

		<CollectionView x:Name="ItemCollectionView"
                    Margin="10"
                    ItemsSource="{Binding DisplayedItems}">

			<CollectionView.ItemsLayout>
				<GridItemsLayout Orientation="Vertical"
                             Span="4"
                             HorizontalItemSpacing="8"
                             VerticalItemSpacing="8"/>
			</CollectionView.ItemsLayout>

			<CollectionView.ItemTemplate>
				<DataTemplate>
					<Frame Padding="5"
                       CornerRadius="8"
                       BackgroundColor="White"
                       HasShadow="True">
						<Frame.GestureRecognizers>
							<TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type local:Buy}}, Path=CardTappedCommand}"
                                              CommandParameter="{Binding .}" />
						</Frame.GestureRecognizers>
						<VerticalStackLayout>
							<Image Source="{Binding ImageData}" 
                               Aspect="AspectFill" 
                               HeightRequest="70"/>
							<Label Text="{Binding Name}" 
                               FontSize="12" 
                               FontAttributes="Bold"
                               LineBreakMode="TailTruncation"/>
							<Label Text="{Binding Price, StringFormat='{0:C}'}" 
                               FontSize="10" 
                               TextColor="Gray"/>
						</VerticalStackLayout>
					</Frame>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>
	</VerticalStackLayout>
</local:PageWithNavBar>